#include<stdio.h>



int main() {
	
	
	/*
	int a = 40
	int b =32
	
	a > b;
	a < b;
	a >= b;
	a >= b;
	a /= b;
	*/
	
	
	
	/*
	
	int a = 40;
	int b = 50;
	int c = 60;
	int d = 70;
	
	a > c && c <=d && (a > b // b < d)
	
	!( a >= d)
	
	
	*/
	
	
	int a,b =20,c =30;
	
	a = b <c  ? b : c;
	printf("%d",a);
	
	
	/*
	
	&& -----> mantıksal and ("ve") bağlcı true && true && flase gibi bağlanabilir && olduğu cümlelerin true olması için bütün ifadelerin true olması
	gerekir.Eğer bu cümlenin içinde bir tane bile false varsa o mantıksal cümle false(yanlış) olur.
	
	
	// -----> mantıksal or ("ya da") bağlacı true // true // false gibi bağlanabilir // olduğu cimlenintrue olması için en az bir ifadenin true olması 
	gerekir.Eğer mantıksal or (//) ile bağnmış cümlenin içinde bütün ifadeler false ise o mantıksal cümle false (yanlış) olur.
	
	! -----> mantıksal not ("değili") operatörü doğru (true) olan mantıksal ifadeyi yanlış (false) yanlış olan mantıksal ifadeyi doğru (true) yapar.
	
	şartlı cümleler
	
	a = x > y ? x : y;
	
	*/
	
	
	return 0;
}
